import React from 'react';
import {Container} from 'lib_components';
import Printables from '@screens/Printables';

export const UserModeHome: React.FC = ({}) => {
  return (
    <Container isSafeAreaView fullFlex>
      {/* <Printables /> */}
    </Container>
  );
};

export default UserModeHome;
