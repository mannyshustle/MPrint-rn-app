import React from 'react';
import {Container} from 'lib_components';

export const ShopManagerModeHome: React.FC = ({}) => {
  return <Container isSafeAreaView fullFlex></Container>;
};

export default ShopManagerModeHome;
